name: Build
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Install dependencies
        run: npm install

      - name: Build app
        run: npm run build